{
  "name": "sunscreen_up",
  "version": "1.0.0",
  "description": "Uses the OpenUV API to tell the user if they need to wear sunscreen based on location",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ashley Manese",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.7",
    "express": "^4.18.3"
  }
}
